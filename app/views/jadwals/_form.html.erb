<%= form_for(jadwal) do |f| %>
  <% if jadwal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jadwal.errors.count, "error") %> prohibited this jadwal from being saved:</h2>

      <ul>
      <% jadwal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
  <%= f.label :waktu %>
  <%= f.text_field :waktu %>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
